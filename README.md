# House-Prices
6.11(评估类特征 + 岭回归)

使用18个评估类的特征作为建模特征，将每个特征值与SalePrice做箱线图，并调整顺序.

处理缺失值后确定模型(岭回归)最佳参数，提交。

最终得分：0.22890


6.11 v2(评估类特征 + 随机森林)

和上面一样的建模过程不过最后的模型换成了随机森林模型

最终得分: 0.22914

可见并没有比岭回归更好所以接下来主要集中在特征工程上, 模型还是随机森林.
